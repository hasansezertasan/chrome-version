# Simple example

Detect the installed Google Chrome version and print it.

## Run

```sh
uv run --locked python examples/simple/main.py
```

## Expected output

```text
Detected Google Chrome 123.0.6312.86
```

If Chrome is not installed, it prints a "not detected" message instead.
